import React from 'react'

export const ContentLayout = ({ children }) => {
    return (
        <div className="bg-programming-icons flex justify-left items-center p-10 w-full">
            {children}
        </div>
    )
}
